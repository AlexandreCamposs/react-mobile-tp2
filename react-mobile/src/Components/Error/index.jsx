import React from "react";
import SweetAlert from "react-native-sweet-alert";
import { Container, Title } from "./styles";

const Error = ({ message, type, title }) => {
  return (
    <Container>
      <Title>TEster</Title>
    </Container>
  );
};

export default Error;
